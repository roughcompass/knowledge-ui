# Exact versions are load-bearing, not a preference. Every module listed in
# mf.shared.ts must resolve to a byte-identical version across all workspaces;
# a caret range lets the host and a remote satisfy the same share with two
# different builds, and Module Federation will quietly load both.
save-exact=true
engine-strict=true
audit=false
fund=false
